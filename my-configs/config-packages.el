;; initial packages

;; git
(use-package magit
  :ensure t
  )

;; vterm & multi-vterm
(use-package vterm
  :ensure t
  )

(use-package multi-vterm
  :ensure t
  )

;; which key
(use-package which-key
  :ensure t
  :config
  (which-key-mode)
 )
